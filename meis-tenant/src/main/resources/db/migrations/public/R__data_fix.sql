-- =============================================================================
-- public schema 数据更正（可重复迁移 R__）
-- =============================================================================
-- 槽位：R__data_fix.sql
-- 约定：
--   1. 建表 / 索引 → 只改 V1__tables.sql、V2__indexes.sql
--   2. 【菜单】禁止写本文件 → 唯一维护点：R__menus.sql（PLT-MENU-01）
--   3. 本文件仅：非菜单数据更正、平台表 ADD COLUMN IF NOT EXISTS
--   4. 不要在本文件 COMMENT ON（注释在 V1/V4）；禁止再新建 V5+/V20+
-- =============================================================================

-- 菜单相关 SQL 已全部迁入 R__menus.sql；此处保留非菜单数据修正槽位。
