# Copy this file to env.txt and edit paths for YOUR machine.
# Lines starting with # are comments. No spaces around = preferred.

# JDK 17 home (folder that contains bin\java.exe)
JAVA_HOME=C:\Program Files\Java\jdk-17

# Maven home (folder that contains bin\mvn.cmd)  OR full path to mvn.cmd
MAVEN_HOME=D:\JAVA\apache-maven-3.8.4
# MAVEN_CMD=D:\JAVA\apache-maven-3.8.4\bin\mvn.cmd

# Node.js (for meis-web production build during 完整打包.bat). Optional if npm is on PATH.
# NODE_HOME=C:\Program Files\nodejs
# NPM_CMD=C:\Program Files\nodejs\npm.cmd
# Skip frontend build (JARs only): SKIP_FRONTEND_BUILD=1
SKIP_FRONTEND_BUILD=0

# Flutter SDK (for 打包apk.bat). Folder that contains bin\flutter.bat
# FLUTTER_ROOT=E:\flutter
# Pub cache on same drive as meis-mobile (avoids Kotlin C:/E: different-roots crash)
# PUB_CACHE=E:\workspace\sb\.pub-cache

# Android SDK (for 打包apk.bat). Or run 安装AndroidSDK.bat first.
# ANDROID_HOME=C:\Users\Administrator\AppData\Local\Android\Sdk
# If Google download times out: script tries Tencent mirror; or open Android Studio SDK Manager;
# or: powershell -File setup-android-sdk.ps1 -ZipPath D:\downloads\commandlinetools-win-*_latest.zip

# Ops panel (used by 启动运维.bat / 运维助手.ps1)
OPS_TOKEN=meis-ops
OPS_PORT=5098
PROFILE=dev

# Database / middleware (passed through to java process environment)
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=meis
POSTGRES_USER=med
POSTGRES_PASSWORD=med123456
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
MINIO_ENDPOINT=http://127.0.0.1:9100
