---
description: 需求先审查再文档再开发；约定沉淀、衍生提示、待开发池、跨模块推广
alwaysApply: true
---

# 需求协作与交付流程（附录 Q）

收到用户需求时，**默认**按下列顺序执行，无需用户再次强调。详见 `docs/meis-requirements.md` 附录 Q。

## 1. 合理性审查（先于改代码）

- 先判断需求是否合理、可落地。
- 若存在风险、歧义、漏洞或与现有架构冲突：先提醒用户并澄清，**不要直接开发**。
- 与用户明确完善后，将定稿写入 `docs/meis-requirements.md`（附录或对应章节），再开发。

## 2. 衍生需求与约定沉淀（审查时一并做）

- **衍生需求**：主需求本身 OK，但经验上常连带相邻能力时，**主动提醒**；确认后先写入文档或第 7 章待开发池，再排期。
- **约定沉淀**：需求适合固化为开发约定时，与用户确认后写入附录 Q（或技术附录），并更新本规则（如需要）。
- **双写可复用包**：凡可跨项目复用的约定，写入 `meis-requirements.md` 的同时，必须同步补充到 `docs/reusable-engineering-conventions.md`（抽象为通用原则；MEIS 专用细节只在该文件「落地映射」中链回）。
- **跨模块推广**：同一思路可提升其他模块时，先提示范围；确认后再批量修补。

## 3. 按文档开发

- 以文档定稿为准实现或修改功能。

## 4. 交付前自检与修补

修改完成后必须检查并补全/修正：

- **后端 CRUD**：列表/详情、创建、更新、删除、业务校验、权限、软删、关联约束、错误返回；**读接口须过滤已软删**
- **前端交互**：操作入口、表单/抽屉、只读与可编边界、按钮显隐、提示、字典/外键中文、与 API 契约一致；**状态/枚举须中文**（约定包 §5.1 / §5.13，禁止裸码直出）；**强业务主数据外键须「编码 名称」**（约定包 §5.2.1 / PLT-REF-CODE-01）
- **新状态/枚举（PLT-DICT-SEED-01）**：新增或扩展状态机/结果码/分类枚举时，同一变更须写字典种子 + 同步 Web/App/MP 静态 catalog + 正确 `dictType`，尽量避免 `未知(码)`
- **列表勾选（约定包 §5.4 / 附录 V）**：有勾选列须跨页缓存；导出/批量变更须选「仅勾选行 / 全部查询结果」；**勿**再挂「已选 N 条 / 全选当页 / 取消全选」提示条；无批量能力勿挂空勾选列
- **主数据业务 Sheet（约定包 §5.16 / PLT-DEV-SHEET-01）**：单据/计划类 Sheet 列表列对齐业务列表关键表头；状态中文；行可下钻详情并见最底层明细；无独立详情页时至少只读抽屉头+明细；**混合来源须靠后加「生成方式」列**（AST-SRC-01）
- **主数据真变更/纠错（约定包 §5.17）**：会形成时间线的字段（科室、楼层/房间等）禁止静默改；按钮弹窗二选一真变更/纠错；补录历史默认不改当前值
- **历史补录确认生效（§5.17 / AST-BF-01）**：补录区间/非维修换件等须保存为草稿再确认；生效后不可改删；勿把该模式套到常规编辑或运维配置 Sheet
- **主数据变更记录（附录 T）**：若新增主数据表/类型，须接入 `EntityChangeLogService`，并把精简快照字段写入附录 **T.5**，与代码 `SNAPSHOT_FIELDS` 一致
- **新表通检**：见约定包 §7.4 / 需求附录 M.7；库表变更必须进 **固定迁库槽位**（附录 D.6），禁止碎片脚本
- **主从单据**：禁止只存主表丢明细（约定包 §6.1）
- **姓名冗余（约定包 §3 / §6.3 / 附录 W.5）**：写 `created/updated/deleted_by` 或业务责任人 UUID 时须同事务写姓名快照；新表配套 `*_by_name` / `{role}_name`；优先 `SoftDeleteSupport`，禁止新增只写 UUID 的入口
- **单据冗余（约定包 §6.2 / 附录 W.6）**：新主从单据明细/子表须冗余业务单号；有 `*_id` 时冗余编码/名称；按需冗余头字段快照，禁止整表头拷贝；状态机字段默认不下沉
- **多端途径（约定包 §5.10 / MOB-CHANNEL-03/04）**：多端写须加 `*_channel` 并写库+展示；Web 追加移动端时须同批历史 NULL 回填为 `web`（§5.10.6），未发生动作勿盲填；**App/小程序一旦做了增删改，交付前须扫描补强途径**；扫描发现**缺途径列须先补列再写落库**；写业务表途径时**同事务**写变更/单据日志 `client`（§5.10.5）
- **多端功能演进同批（约定包 §5.18 / MOB-SYNC-01）**：Web 与移动端**已共有**的功能，若发生字段扩展或业务分支扩展，须同批改 App/小程序（已有入口的端），禁止只改 Web 回头再补；暂缓须入 BACKLOG 并点名

发现缺口当场修补后再结束。

## 5. 待开发功能池（第 7 章）

- 合理但暂无时间开发/测试的需求：记入第 7 章，编号 `BACKLOG-{模块}-{序号}`。
- 勿与第 4 章缺陷、第 5 章待确认问题混放。

## 6. 说明

- 需求已足够明确且无风险时：可简要确认要点 → 写入文档 → 开发。
- 有疑问仍须先对齐再动手。
- 技术约定索引见附录 **Q.6**；约定沉淀增量见 **Q.7 / Q.8**。
- 跨项目可复用约定全集见 `docs/reusable-engineering-conventions.md`（当前 v1.48）。
