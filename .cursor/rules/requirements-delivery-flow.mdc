---
description: 需求先审查再文档再开发，交付前检查并修补前后端 CRUD/交互
alwaysApply: true
---

# 需求协作与交付流程（附录 Q）

收到用户需求时，**默认**按下列顺序执行，无需用户再次强调。

## 1. 合理性审查（先于改代码）

- 先判断需求是否合理、可落地。
- 若存在风险、歧义、漏洞或与现有架构冲突：先提醒用户并澄清，**不要直接开发**。
- 与用户明确完善后，将定稿写入 `docs/meis-requirements.md`（附录或对应章节），再开发。

## 2. 按文档开发

- 以文档定稿为准实现或修改功能。

## 3. 交付前自检与修补

修改完成后必须检查并补全/修正：

- **后端 CRUD**：列表/详情、创建、更新、删除、业务校验、软删、关联约束、错误返回等
- **前端交互**：操作入口、表单/抽屉、只读与可编边界、按钮显隐、提示、与 API 契约一致

发现缺口当场修补后再结束。

## 4. 说明

- 需求已足够明确且无风险时：可简要确认要点 → 写入文档 → 开发。
- 有疑问仍须先对齐再动手。
